language: php
php:
  - "7.0"
services:
  - mysql
before_script:
  - "mysql -e 'create database IF NOT EXISTS `doc`;'"
  - "mysql -e 'source tests/doc.sql;'"
  - composer self-update
  - composer update