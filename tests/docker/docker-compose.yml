version: '2'
services:
    mysql:
      image: mysql
      networks:
        comm:
          aliases:
            - mysql
      environment:
          MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
      ports:
        - "3306:3306"
        - "22"

networks:
    comm: