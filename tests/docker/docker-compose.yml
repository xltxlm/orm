version: '2'
services:
    mysql:
      image: mysql
      volumes:
              - ../../:/orm/
      networks:
        comm:
          aliases:
            - mysql
      environment:
          MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
    php:
      image: php
      volumes:
              - ../../:/orm/
      entrypoint: tail -f /etc/issue
      networks:
        comm:
          aliases:
            - php
networks:
    comm: